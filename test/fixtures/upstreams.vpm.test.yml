default:
  - baseUrl: https://gitlab.example.com
upstreams:
  - baseUrl: https://vpm.example.com/index.json
    scopes:
      - com.example.vpm.*
      # A scoped name, so the global tarball route can be exercised with one.
      - "@vpmscope/*"
    type: vpm
